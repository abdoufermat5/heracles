# RFC2307bis Style - Auxiliary posixGroup (LDIF format)
# =======================================================
# For loading into OpenLDAP via cn=config
#
dn: cn=hrc-aux,cn=schema,cn=config
objectClass: olcSchemaConfig
cn: heracles-aux
olcObjectClasses: ( 1.3.6.1.4.1.99999.1.2.1 
  NAME 'posixGroupAux'
  DESC 'Auxiliary POSIX group class for mixed groups'
  SUP top
  AUXILIARY
  MUST gidNumber
  MAY ( cn $ userPassword $ memberUid $ description ) )
